import React, { useState } from 'react';
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';

const scannerAddScreen = ({ navigation }) => {
    return (
      <View>
        <Text>Hello Camera</Text>
      </View>
    );
  }

export default scannerAddScreen;